## جرد الخصائص المطلوبة

هذا المستند يحصر البنود المذكورة في القائمة المرجعية (الصور المرسلة) ويقارنها بالوضع الحالي للمنصة.

> **Legend**
> - ✅ متوفر حالياً
> - 🟡 متوفر جزئياً / يحتاج تطوير تكميلي
> - ❌ غير متوفر (يحتاج بناء كامل)

| القسم | الميزة | الحالة الحالية | ملاحظات |
|-------|--------|----------------|----------|
| نظام الكورسات | إنشاء الكورسات (اسم، وصف، سعر، خصم، صورة، مدرس) | ✅ | الكيان `AdvancedCourse` يدعم الحقول الأساسية، مع إدارة عبر لوحات الإدارة. |
| نظام الكورسات | تقسيم الكورس لمحاضرات (فيديوهات، ملفات، كويزات، تمارين) | ✅ | متوفر عبر `CourseLesson` وأنواع الدروس (video/document/quiz/assignment). |
| نظام الكورسات | تحديد صلاحيات الوصول (مدفوع / مجاني / بالتقسيط) | 🟡 | مجاني/مدفوع مدعوم (حقول السعر، التفعيل). نظام التقسيط غير موجود. |
| نظام الكورسات | تتبع تقدم الطالب (progress tracking) | ✅ | متوفر عبر `LessonProgress` و`VideoWatch`. العرض يحتاج تحسينات UI. |
| نظام إدارة المحتوى (CMS) | إدارة المقالات في المدونة | ✅ | متوفر عبر `BlogPost` ولوحات الإدارة. |
| نظام إدارة المحتوى (CMS) | إدارة الأسئلة الشائعة | ✅ | نموذج `FAQ` موجود. |
| نظام إدارة المحتوى (CMS) | التحكم في الصفحات العامة | 🟡 | توجد صفحات Blade ثابتة؛ لا يوجد محرك صفحات ديناميكي كامل. |
| نظام الفوترة والمحاسبة | تسجيل كل عملية دفع (كاش، تحويل، أونلاين) | 🟡 | نماذج `Payment`, `Invoice`, `Order` موجودة. لا يوجد تتبع مفصل لنوع المحفظة أو الربط الآلي. |
| نظام الفوترة والمحاسبة | نظام أقساط (عدد الأقساط + تاريخ كل قسط) | ❌ | غير متوفر. |
| نظام الفوترة والمحاسبة | إشعارات آلية قبل موعد الدفع | ❌ | غير متوفر. |
| نظام الفوترة والمحاسبة | تقارير مالية شاملة (داخل/خارج) | 🟡 | توجد بعض الإحصائيات، لكن لا تقارير محاسبية مفصلة. |
| نظام الفوترة والمحاسبة | ربط بوابة دفع إلكتروني/محافظ | ❌ | لا يوجد تكامل حالي مع بوابات دفع. |
| نظام المحاضرات والجروبات | رفع تسجيلات المحاضرات وربط التيمز | 🟡 | هناك موديل `Lecture` وحقول روابط، لكن لا تكامل أوتوماتيكي مع Teams. |
| نظام المحاضرات والجروبات | حضور وانصراف الطلاب + عدد ساعات | 🟡 | يوجد `AttendanceRecord`/`AttendanceStatistics` لكن التتبع الآلي غير مكتمل. |
| نظام المحاضرات والجروبات | واجبات وتقييمات لكل محاضرة مع ربط التسجيل | 🟡 | توجد موديلات `LectureAssignment` و`LectureAssignmentSubmission` لكن تحتاج تفعيل/واجهات. |
| نظام الفوترة والمحاسبة | نظام تتبع عمليات الدفع الذكي (محافظ متعددة، مطابقة مبالغ) | ❌ | غير متوفر. يتطلب تصميم جداول وسير عمل جديد. |
| نظام الفوترة والمحاسبة | تقارير يومية/شهرية للأرصدة في كل محفظة | ❌ | غير متوفر. |
| نظام الفوترة والمحاسبة | إشعارات فرق المبلغ المفترض والمستلم | ❌ | غير متوفر. |
| خصائص إضافية | شهادات إتمام الكورس تلقائياً | 🟡 | يوجد موديل `Certificate` لكن الأتمتة/التوليد بحاجة مراجعة. |
| خصائص إضافية | واجهة استخدام عربية/إنجليزية | 🟡 | هناك محتوى عربي فقط؛ لا يوجد دعم تعدد لغات حقيقي. |
| خصائص إضافية | نظام تقييم الكورسات والمدرسين | 🟡 | موديل `CourseReview` موجود لكن يحتاج تمكين في الواجهة. |
| خصائص إضافية | نظام أمان (SSL، حماية تسجيل الدخول) | 🟡 | يعتمد على إعداد الخادم؛ التطبيق يدعم سياسات أساسية لكن يلزم إعدادات بنية تحتية. |
| خصائص إضافية | نسخ احتياطي دوري للبيانات | ❌ | يحتاج إعداد خارجي أو أوامر مجدولة. |
| خصائص إضافية | ربط Google Calendar لعرض مواعيد المحاضرات | ❌ | غير متوفر. |
| خصائص إضافية | نظام تذاكر الدعم الفني (Support Tickets) | ❌ | لا يوجد نموذج أو واجهة حالياً. |
| خصائص إضافية | إشعارات بالبريد أو SMS | 🟡 | هناك `Notification` و`MessageTemplate` لكن لا توجد تكاملات خارجية نشطة. |
| خصائص إضافية | نظام أكواد خصم وكوبونات | ✅ | موديلات `Coupon` و`CouponUsage` متاحة. |

### خلاصة أولية

- تم تغطية الأساسيات الخاصة بالكورسات وCMS وبعض عناصر الفوترة.
- تتطلب أنظمة الدفع الذكي، الربط بالمحافظ، Google Calendar، الدعم الفني، النسخ الاحتياطي، وغيرها عملاً تطويرياً كاملاً.
- ينصح بتحديد أولويات التنفيذ وجدولة العمل على مراحل (مع قصص مستخدمين لكل خاصية).


