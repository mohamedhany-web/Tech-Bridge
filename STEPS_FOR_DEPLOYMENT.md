# خطوات الرفع على الاستضافة - ضمان عمل الصور

## ✅ خطوات مهمة بعد الرفع على الخادم:

### 1. إنشاء رابط Storage
```bash
php artisan storage:link
```

### 2. التحقق من صلاحيات المجلدات
```bash
chmod -R 775 storage
chmod -R 775 bootstrap/cache
```

### 3. تحديث APP_URL في .env
```env
APP_URL=https://yourdomain.com
```

### 4. مسح الكاش
```bash
php artisan config:clear
php artisan cache:clear
php artisan view:clear
php artisan route:clear
```

## 🔧 إذا لم تعمل الصور:

### الحل البديل: Route مخصص
تم إضافة route في `routes/web.php` لعرض الملفات من storage:

```
/storage/{path}
```

هذا الـ route سيعمل حتى لو فشل الرابط الرمزي.

### التحقق من عمل الصور:
1. افتح المتصفح
2. افتح DevTools (F12)
3. اذهب إلى Network
4. حاول فتح الصورة
5. تحقق من status code (يجب أن يكون 200)

## 📋 قائمة التحقق:

- [ ] تحديث APP_URL في .env
- [ ] تشغيل `php artisan storage:link`
- [ ] التحقق من صلاحيات storage و bootstrap/cache
- [ ] مسح جميع أنواع الكاش
- [ ] اختبار رفع صورة جديدة
- [ ] اختبار عرض صورة موجودة
- [ ] التحقق من أن route `/storage/{path}` يعمل

